import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Payment = () => {

  useTitle('Payment Options');

  return (
    <div>
      <h2 className='text-center'>Payment Options coming soon!</h2>
      <p>Stay Tuned!</p>
    </div>
  );
};

export default Payment;